import { mkdir } from "node:fs/promises";
import { build } from "../vir/node_modules/esbuild/lib/main.js";

await mkdir(new URL("../player_js/generated/", import.meta.url), { recursive: true });
await build({
    entryPoints: [new URL("../player_js/animate_vir_widget.js", import.meta.url).pathname],
    outfile: new URL("../player_js/generated/animate_vir_widget.js", import.meta.url).pathname,
    bundle: true,
    format: "esm",
    platform: "browser",
    target: "es2022",
    external: ["react", "@leanprover/infoview"],
    banner: { js: "// Generated by scripts/build-vir-widget.mjs." },
});
